name: Staging CI

on:
  push:
    branches: [ staging ]

permissions:
  contents: read
  packages: write

jobs:
  staging-build:
    uses: ./.github/workflows/docker-build-template.yml
    with:
      tag_prefix: ''
      branch: 'staging'