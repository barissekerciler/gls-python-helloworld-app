name: Production CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  production-build:
    uses: ./.github/workflows/docker-build-template.yml
    with:
      tag_prefix: 'v'
      branch: 'master'